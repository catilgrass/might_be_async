[package]
name = "might_be_async_test_sync"
version = "0.1.0"
edition = "2024"

[features]
sync = []
metadata_async = []
custom_ft = []
custom_invoke = []

[package.metadata.might_be_async]
default_feature_name = "metadata_async"

[dependencies]
might_be_async = { path = "../.." }
